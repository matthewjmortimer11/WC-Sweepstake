{
  "name": "wheesht-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend build for Wheesht. Transpiles JSX ahead of time so the browser never loads Babel.",
  "scripts": {
    "build": "node scripts/build_frontend.mjs",
    "build:watch": "node scripts/build_frontend.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
